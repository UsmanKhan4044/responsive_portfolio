import 'package:flutter/material.dart';

const primaryColor = Color(0xFFFFC107);      // Yellow accent color
const secondaryColor = Color(0xFF242430);    // Card background
const darkColor = Color(0xFF191923);         // Dark background
const bodyTextColor = Color(0xFF8B8B8D);     // Body text color
const bgColor = Color(0xFF1E1E28);           // Main background

const defaultPadding = 20.0;                 // Standard padding
const defaultDuration = Duration(seconds: 1); // Animation duration
const maxWidth = 1440.0;                     // Max web width
